# Ansible inventory for graphmcp deployment targets
[graphmcp_servers]
# replace with real hosts, e.g.:
# prod-tools-01 ansible_host=192.168.1.50 ansible_user=deploy
localhost ansible_connection=local

[graphmcp_servers:vars]
install_dir=/opt/graphmcp
