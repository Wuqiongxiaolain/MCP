---
name: "代码审查报告_2026_07_14：MCP 性能与设计审查"
overview: 对 graphmcp 做静态证据审查，并在隔离临时存储中运行基准、压力与故障注入；最终用可视化 Canvas 汇总分级问题、实测数据和改进优先级，不修改项目源码。
todos:
  - id: static-audit
    content: 完成六个维度的静态证据矩阵与调用路径分析
    status: completed
  - id: baseline-tests
    content: 运行现有测试和基准并记录当前环境结果
    status: completed
  - id: stress-fault-tests
    content: 在隔离存储中执行尺度、并发、内存与故障注入
    status: completed
  - id: publish-audit
    content: 生成可视化 Canvas 审查报告与优先级建议
    status: completed
isProject: false
---

# 代码审查报告_2026_07_14：MCP 性能与设计审查

## 审查范围
- 沿 `[src/mcp.hpp](D:/Schoolworks/Coding/DevOps/MCP-/src/mcp.hpp)` 的 stdio JSON-RPC 主链路，交叉检查 `[src/json.hpp](D:/Schoolworks/Coding/DevOps/MCP-/src/json.hpp)`、`[src/storage.hpp](D:/Schoolworks/Coding/DevOps/MCP-/src/storage.hpp)`、`[src/version_manager.hpp](D:/Schoolworks/Coding/DevOps/MCP-/src/version_manager.hpp)`、`[src/exporters.hpp](D:/Schoolworks/Coding/DevOps/MCP-/src/exporters.hpp)` 与表工具实现。
- 逐项覆盖：内存/资源生命周期、JSON 编解码与复制、高并发/多写者、超时与错误边界、46 个工具的调用经济性、无额外提示词时的可理解性与失败重试循环。

## 静态审查
- 为每项发现记录严重度、证据行号、触发条件、影响、现有保护与修复方向；区分经典内存泄露、峰值内存和磁盘资源累积。
- 审查 `[src/mcp.hpp](D:/Schoolworks/Coding/DevOps/MCP-/src/mcp.hpp)` 中的 tool schema/description/返回格式，并与 `[docs/CLI_MCP_REFERENCE.md](D:/Schoolworks/Coding/DevOps/MCP-/docs/CLI_MCP_REFERENCE.md)`、`[docs/api_reference/openapi.yaml](D:/Schoolworks/Coding/DevOps/MCP-/docs/api_reference/openapi.yaml)` 和 `[mcp-config.example.json](D:/Schoolworks/Coding/DevOps/MCP-/mcp-config.example.json)` 对照，定位 enum、互斥参数、JSON-in-string、output schema、错误 hint 等缺口。
- 用典型中等任务计算最少调用数：创建并导出、批量改图并提交、规则生成/校验/修复、图表互转、游标遍历；标出可合并 pipeline 与已有批处理捷径。

## 隔离动态验证
- 先运行现有单元、MCP smoke 与 `[tests/bench_main.cpp](D:/Schoolworks/Coding/DevOps/MCP-/tests/bench_main.cpp)` 基准，对照 `[tests/bench_baseline.json](D:/Schoolworks/Coding/DevOps/MCP-/tests/bench_baseline.json)`；不更新基线。
- 使用临时 `GRAPHMCP_STORE` 做端到端尺度测试：不同节点数/载荷大小下记录 parse、工具调用、响应体积、p50/p95、吞吐与进程 RSS；Windows RSS 用进程采样补足现有基准会跳过的内存项。
- 做受控压力与故障注入：流水线请求下慢工具对 `ping` 的阻塞、超大单行消息、多进程共享 store 写入、外部导出程序挂起/超时、写盘中断后的 JSON 完整性。所有测试仅使用临时目录，并设置外层 watchdog，避免测试自身无限挂起。
- 以最小/错误参数执行高风险工作流，观察错误是否给出可行动的 `code/hint/next_tool`，并验证 `commit→stage`、JSON 字符串参数、create/import、checkout/draft 等场景是否容易形成无进展重试。

## 交付物
- 创建独立可视化审查 Canvas，包含：总体风险评级、六个维度结论、静态证据、实测指标、典型失败循环、调用数对比及 P0/P1/P2 改进路线；明确区分“已实测”和“静态推断”。
- 给出短结论及 Canvas 链接；不改源码、不提交、不触碰当前未跟踪的 `[.github/workflows/deploy.yml](D:/Schoolworks/Coding/DevOps/MCP-/.github/workflows/deploy.yml)`。若某项因当前 Windows 环境或外部工具缺失无法验证，会在报告中标记限制而不伪造结论。